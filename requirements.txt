# Core web UI
gradio>=4.44
chainlit
python-dotenv


# LangChain ecosystem (use the unified langchain packages where possible)
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-text-splitters>=0.3.0
langchain-ollama>=0.2.0
langchain-huggingface>=0.1.0
langchain-chroma>=0.1.4
langchain-openai>=0.2.0

# Vector store
chromadb>=0.5.5

# PDF loading
pypdf==6.0.0

# Embeddings (sentence-transformers)
sentence-transformers>=3.1.0

# Hugging Face hubs & transformers (needed for local HF pipeline option)
transformers
torch>=2.3.0          # or torch>=2.3.0+cu121 if you use CUDA
accelerate>=0.33.0

# Optional but recommended
tokenizers>=0.19.1
tqdm>=4.66.0

# For OpenRouter / external API usage (already included in langchain-openai, but safe to pin)
httpx>=0.27.0
openai>=1.40.0        # langchain-openai depends on this
